package utility

import data.Vehicle

class XmlWriter {
    //todo: replace with Jackson
    //todo: doesn't work with current gradle build

    /**
     * writes collection in xml format
     */
    fun write(vehicles: Collection<Vehicle>, path: String) {
       TODO()
    }

}